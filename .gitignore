# Dependencies
node_modules/
package.json
package-lock.json

# El proyecto Expo SÍ necesita su manifest y lockfile en git (EAS Build
# los lee del archivo subido). Se exceptúan de la regla global de arriba.
!apps/bipi-mobile/package.json
!apps/bipi-mobile/package-lock.json

# Next.js build output
.next/
out/

# Build info
*.tsbuildinfo

# Env files
.env
.env.local
.env*.local

# Logs
*.log
npm-debug.log*
