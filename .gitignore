# Dependencies
node_modules/
package.json
package-lock.json

# Sólo el package.json/lock ACCIDENTAL de la raíz del repo (ancla "/").
# OJO: sin la barra inicial el patrón matchea package.json en CUALQUIER
# subcarpeta y rompe `railway up` (excluye agencia-platform/package.json
# del build context → "COPY package.json: not found") y EAS Build.
/package.json
/package-lock.json

# Next.js build output
.next/
out/

# Expo / React Native build output
apps/bubui-mobile/android/
apps/bipi-mobile/android/
apps/bubui-mobile/.expo/
apps/bipi-mobile/.expo/

# Build info
*.tsbuildinfo

# Env files
.env
.env.local
.env*.local

# Logs
*.log
npm-debug.log*
