/* Negocio Vivo Leads — JS común admin (extensible) */
(function($){
    'use strict';

    $(document).ready(function(){
        // Reservado para interacciones globales del plugin.
        // El detalle de lead trae su JS embebido por dependencia de variables PHP.
    });

})(jQuery);
