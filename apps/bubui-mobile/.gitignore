# Expo / build artifacts (generados por prebuild, no versionar)
/android
/ios
.expo/
dist/
web-build/
node_modules/
*.log
.DS_Store

# Firebase / FCM (se inyecta desde secret en CI; no commitear)
google-services.json

# Credencial de Google Play para 'eas submit' (NO subir)
google-service-account.json

# Credenciales iOS locales (NO subir)
credentials.json
dist.p12
dist.mobileprovision
*.p8
dist-ext.mobileprovision
